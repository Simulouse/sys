<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sim.sys.dao.MedicineDao">

    <resultMap type="com.sim.sys.entity.Medicine" id="medicineMap">
        <result property="medicineId" column="medicine_id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="NUMERIC"/>
        <result property="restNums" column="rest_nums" jdbcType="INTEGER"/>
        <result property="expiredTime" column="expired_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="medicineMap">
        select
            medicine_id, name, price, rest_nums, expired_time
        from medicine_management.tb_medicine
        where medicine_id = #{medicineId}
    </select>
    <select id="queryNumById">
        select rest_nums
        from medicine_management.tb_medicine
        where medicine_id = #{medicineId}
    </select>
    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="medicineMap">
        select
            medicine_id, name, price, rest_nums, expired_time
        from medicine_management.tb_medicine
                 limit #{offset}, #{limit}
    </select>

    <select id="queryExpiredMedicine" resultMap="medicineMap">
        select medicine_id, name, price, rest_nums, expired_time
        from medicine_management.tb_medicine where  DATEDIFF(expired_time,now())<0
    </select>
    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="medicineMap">
        select
        medicine_id, name, price, rest_nums, expired_time
        from medicine_management.tb_medicine
        <where>
            <if test="medicineId != null and medicineId != ''">
                and medicine_id = #{medicineId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="restNums != null">
                and rest_nums = #{restNums}
            </if>
            <if test="expiredTime != null">
                and expired_time = #{expiredTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="medicineId" useGeneratedKeys="true">
        insert into medicine_management.tb_medicine(medicine_id , name, price, rest_nums, expired_time)
        values (#{medicineId},#{name}, #{price}, #{restNums}, #{expiredTime})
    </insert>

    <insert id="insertBatch" keyProperty="medicineId" useGeneratedKeys="true">
        insert into medicine_management.tb_medicine(name, price, rest_nums, expired_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.price}, #{entity.restNums}, #{entity.expiredTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="medicineId" useGeneratedKeys="true">
        insert into medicine_management.tb_medicine(name, price, rest_nums, expired_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.price}, #{entity.restNums}, #{entity.expiredTime})
        </foreach>
        on duplicate key update
        name = values(name) , price = values(price) , rest_nums = values(rest_nums) , expired_time =
        values(expired_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update medicine_management.tb_medicine
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="restNums != null">
                rest_nums = #{restNums},
            </if>
            <if test="expiredTime != null">
                expired_time = #{expiredTime},
            </if>
        </set>
        where medicine_id = #{medicineId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from medicine_management.tb_medicine where medicine_id = #{medicineId}
    </delete>

</mapper>

