<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sim.sys.dao.OrderDao">

    <resultMap type="com.sim.sys.entity.Order" id="TbOrderMap">
        <result property="orderId" column="order_id" jdbcType="VARCHAR"/>
        <result property="pharmacistId" column="pharmacist_id" jdbcType="VARCHAR"/>
        <result property="supplierId" column="supplier_id" jdbcType="VARCHAR"/>
        <result property="medicineId" column="medicine_id" jdbcType="VARCHAR"/>
        <result property="nums" column="nums" jdbcType="INTEGER"/>
        <result property="time" column="time" jdbcType="TIMESTAMP"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TbOrderMap">
        select
          order_id, pharmacist_id, supplier_id, medicine_id, nums, time, state
        from medicine_management.tb_order
        where order_id = #{orderId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TbOrderMap">
        select
          order_id, pharmacist_id, supplier_id, medicine_id, nums, time, state
        from medicine_management.tb_order
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TbOrderMap">
        select
        order_id, pharmacist_id, supplier_id, medicine_id, nums, time, state
        from medicine_management.tb_order
        <where>
            <if test="orderId != null and orderId != ''">
                and order_id = #{orderId}
            </if>
            <if test="pharmacistId != null and pharmacistId != ''">
                and pharmacist_id = #{pharmacistId}
            </if>
            <if test="supplierId != null and supplierId != ''">
                and supplier_id = #{supplierId}
            </if>
            <if test="medicineId != null and medicineId != ''">
                and medicine_id = #{medicineId}
            </if>
            <if test="nums != null">
                and nums = #{nums}
            </if>
            <if test="time != null">
                and time = #{time}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="orderId" useGeneratedKeys="true">
        insert into medicine_management.tb_order(pharmacist_id, supplier_id, medicine_id, nums, time, state)
        values (#{pharmacistId}, #{supplierId}, #{medicineId}, #{nums}, #{time}, #{state})
    </insert>

    <insert id="insertBatch" keyProperty="orderId" useGeneratedKeys="true">
        insert into medicine_management.tb_order(pharmacist_id, supplier_id, medicine_id, nums, time, state)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pharmacistId}, #{entity.supplierId}, #{entity.medicineId}, #{entity.nums}, #{entity.time},
            #{entity.state})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="orderId" useGeneratedKeys="true">
        insert into medicine_management.tb_order(pharmacist_id, supplier_id, medicine_id, nums, time, state)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.pharmacistId}, #{entity.supplierId}, #{entity.medicineId}, #{entity.nums}, #{entity.time},
            #{entity.state})
        </foreach>
        on duplicate key update
        pharmacist_id = values(pharmacist_id) , supplier_id = values(supplier_id) , medicine_id = values(medicine_id) ,
        nums = values(nums) , time = values(time) , state = values(state)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update medicine_management.tb_order
        <set>
            <if test="pharmacistId != null and pharmacistId != ''">
                pharmacist_id = #{pharmacistId},
            </if>
            <if test="supplierId != null and supplierId != ''">
                supplier_id = #{supplierId},
            </if>
            <if test="medicineId != null and medicineId != ''">
                medicine_id = #{medicineId},
            </if>
            <if test="nums != null">
                nums = #{nums},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
        </set>
        where order_id = #{orderId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from medicine_management.tb_order where order_id = #{orderId}
    </delete>

</mapper>

