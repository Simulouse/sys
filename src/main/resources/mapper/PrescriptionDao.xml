<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sim.sys.dao.PrescriptionDao">

    <resultMap type="com.sim.sys.entity.Prescription" id="PrescriptionMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="medicineId" column="medicine_id" jdbcType="VARCHAR"/>
        <result property="medicineName" column="medicine_name" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="INTEGER"/>
        <result property="num" column="num" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PrescriptionMap">
        select
          id, medicine_id, medicine_name, price, num
        from medicine_management.prescription
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PrescriptionMap">
        select
          id, medicine_id, medicine_name, price, num
        from medicine_management.prescription
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PrescriptionMap">
        select
        id, medicine_id, medicine_name, price, num
        from medicine_management.prescription
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="medicineId != null and medicineId != ''">
                and medicine_id = #{medicineId}
            </if>
            <if test="medicineName != null and medicineName != ''">
                and medicine_name = #{medicineName}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="num != null">
                and num = #{num}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into medicine_management.prescription(medicine_id, medicine_name, price, num)
        values (#{medicineId}, #{medicineName}, #{price}, #{num})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into medicine_management.prescription(medicine_id, medicine_name, price, num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.medicineId}, #{entity.medicineName}, #{entity.price}, #{entity.num})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into medicine_management.prescription(medicine_id, medicine_name, price, num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.medicineId}, #{entity.medicineName}, #{entity.price}, #{entity.num})
        </foreach>
        on duplicate key update
        medicine_id = values(medicine_id) , medicine_name = values(medicine_name) , price = values(price) , num =
        values(num)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update medicine_management.prescription
        <set>
            <if test="medicineId != null and medicineId != ''">
                medicine_id = #{medicineId},
            </if>
            <if test="medicineName != null and medicineName != ''">
                medicine_name = #{medicineName},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="num != null">
                num = #{num},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from medicine_management.prescription where id = #{id}
    </delete>

</mapper>

